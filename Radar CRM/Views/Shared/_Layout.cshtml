<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Radar One</title>
    <script type="importmap"></script>

    <!-- Standard Styles -->
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/Publisher_CRM.styles.css" asp-append-version="true" />

    <style>
        /* --- Professional Enterprise Palette --- */
        :root {
        --bg-light: #f8fafc; /* Slightly cooler light background */
        --sidebar-bg: #ffffff;
        --sidebar-border: #e2e8f0;
        --sidebar-text: #475569;
        --sidebar-hover-bg: #f1f5f9;
        --sidebar-active-bg: #eff6ff;
        --sidebar-active-text: #1d4ed8; /* Vibrant SaaS Blue */
        --navbar-bg: #ffffff;
        --navbar-border: #e2e8f0;
        --shadow-soft: 0 4px 6px -1px rgba(0, 0, 0, 0.05), 0 2px 4px -1px rgba(0, 0, 0, 0.03);
        }

        /* --- Layout & Structure --- */
        body {
        margin: 0;
        padding: 0;
        overflow: hidden;
        background-color: var(--bg-light);
        font-family: 'Inter', -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Arial, sans-serif;
        font-size: 0.875rem;
        }

        .app-container {
        display: flex;
        height: 100vh;
        width: 100vw;
        }

        /* --- Premium Sidebar Styling --- */
        .sidebar {
        width: 240px; /* Slightly wider for better text fit */
        background-color: var(--sidebar-bg);
        border-right: 1px solid var(--sidebar-border);
        display: flex;
        flex-direction: column;
        flex-shrink: 0;
        z-index: 1040;
        transition: transform 0.3s ease-in-out;
        box-shadow: 2px 0 8px rgba(0,0,0,0.02); /* Soft shadow bleeding into main content */
        }

        .sidebar-brand {
        padding: 0 1.5rem;
        display: flex;
        align-items: center;
        justify-content: space-between;
        height: 70px;
        background-color: var(--sidebar-bg);
        }

        .brand-logo-container {
        display: flex;
        align-items: center;
        gap: 12px;
        }

        .brand-logo-container h5 {
        color: #0f172a;
        font-weight: 700;
        margin: 0;
        letter-spacing: -0.5px;
        font-size: 1.15rem;
        }

        .sidebar-nav {
        padding: 1rem 0.75rem;
        overflow-y: auto;
        flex-grow: 1;
        }

        .sidebar-nav::-webkit-scrollbar {
        width: 4px;
        }

        .sidebar-nav::-webkit-scrollbar-thumb {
        background-color: #cbd5e1;
        border-radius: 4px;
        }

        .nav-section-title {
        font-size: 0.7rem;
        text-transform: uppercase;
        font-weight: 600;
        color: #94a3b8;
        letter-spacing: 0.05em;
        margin: 1.5rem 0 0.5rem 1rem;
        }

        /* Nav Items - Pill Shaped */
        .nav-item-link {
        display: flex;
        align-items: center;
        justify-content: space-between;
        padding: 0.5rem 1rem;
        color: var(--sidebar-text);
        text-decoration: none;
        border-radius: 8px; /* Modern SaaS Pill shape */
        margin-bottom: 4px;
        font-weight: 500;
        font-size: 0.9rem;
        transition: all 0.2s ease;
        }

        .nav-item-link-content {
        display: flex;
        align-items: center;
        }

        .nav-item-link i {
        margin-right: 0.75rem;
        font-size: 1.1rem;
        color: #94a3b8;
        transition: color 0.2s ease;
        }

        /* Hover & Active States */
        .nav-item-link:hover:not(.active) {
        background-color: var(--sidebar-hover-bg);
        color: #0f172a;
        }

        .nav-item-link:hover:not(.active) i {
        color: #64748b;
        }

        .nav-item-link.active {
        background-color: var(--sidebar-active-bg);
        color: var(--sidebar-active-text);
        font-weight: 600;
        }

        .nav-item-link.active i {
        color: var(--sidebar-active-text);
        }

        /* Sub-menu */
        .nav-item-link .toggle-arrow {
        transition: transform 0.3s ease;
        font-size: 0.8rem;
        color: #94a3b8;
        }

        .nav-item-link[aria-expanded="true"] .toggle-arrow {
        transform: rotate(180deg);
        }

        .sub-menu {
        margin-left: 1.25rem;
        padding-left: 0.5rem;
        border-left: 2px solid #f1f5f9;
        margin-top: 4px;
        margin-bottom: 8px;
        }

        .sub-menu .nav-item-link {
        font-size: 0.85rem;
        padding: 0.4rem 1rem;
        color: #64748b;
        }

        /* --- Main Content Area --- */
        .main-wrapper {
        flex-grow: 1;
        display: flex;
        flex-direction: column;
        overflow: hidden;
        }

        .top-navbar {
        height: 70px;
        background-color: var(--navbar-bg);
        border-bottom: 1px solid var(--navbar-border);
        display: flex;
        align-items: center;
        justify-content: space-between;
        padding: 0 2rem;
        flex-shrink: 0;
        z-index: 10;
        }

        .content-area {
        flex-grow: 1;
        overflow-y: auto;
        padding: 2rem;
        background-color: var(--bg-light);
        }

        /* --- Global Search Bar --- */
        .search-wrapper {
        position: relative;
        width: 450px; /* Wider search bar */
        }

        .search-wrapper .bi-search {
        position: absolute;
        left: 16px;
        top: 50%;
        transform: translateY(-50%);
        color: #94a3b8;
        font-size: 0.9rem;
        z-index: 2;
        }

        .search-wrapper input {
        width: 100%;
        padding-left: 40px;
        border-radius: 8px;
        background-color: #f1f5f9;
        border: 1px solid transparent;
        font-size: 0.9rem;
        padding-top: 10px;
        padding-bottom: 10px;
        transition: all 0.2s;
        color: #0f172a;
        }

        .search-wrapper input:focus {
        background-color: #ffffff;
        border-color: #93c5fd;
        box-shadow: 0 0 0 4px rgba(59, 130, 246, 0.1);
        outline: none;
        }

        /* Search Results Dropdown */
        .search-results-dropdown {
        display: none;
        position: absolute;
        top: 110%;
        left: 0;
        width: 100%;
        background: white;
        border: 1px solid var(--border-soft);
        border-radius: 8px;
        box-shadow: var(--shadow-soft);
        z-index: 1050;
        max-height: 400px;
        overflow-y: auto;
        }

        .search-result-item {
        padding: 12px 16px;
        border-bottom: 1px solid #f1f5f9;
        cursor: pointer;
        transition: background 0.2s;
        display: flex;
        align-items: flex-start;
        gap: 12px;
        }

        .search-result-item:last-child {
        border-bottom: none;
        }

        .search-result-item:hover {
        background: #f8fafc;
        }

        .search-result-icon {
        background: #eff6ff;
        color: var(--primary-blue);
        width: 32px;
        height: 32px;
        border-radius: 6px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1rem;
        }

        .search-result-details h6 {
        margin: 0 0 4px 0;
        font-size: 0.9rem;
        font-weight: 600;
        color: #0f172a;
        }

        .search-result-details p {
        margin: 0;
        font-size: 0.8rem;
        color: #64748b;
        }

        .search-badge {
        font-size: 0.65rem;
        padding: 2px 6px;
        border-radius: 4px;
        font-weight: 600;
        text-transform: uppercase;
        }

        .badge-account {
        background: #e0e7ff;
        color: #1e40af;
        }

        .badge-lead {
        background: #dcfce3;
        color: #166534;
        }

        .badge-deal {
        background: #fef3c7;
        color: #92400e;
        }

        /* --- Header Controls --- */
        .header-actions {
        display: flex;
        align-items: center;
        gap: 1.25rem;
        }

        .btn-signout {
        color: #64748b;
        font-size: 0.85rem;
        font-weight: 500;
        text-decoration: none;
        padding: 8px 16px;
        border-radius: 8px;
        border: 1px solid var(--sidebar-border);
        transition: 0.2s;
        background: white;
        }

        .btn-signout:hover {
        background-color: #fef2f2;
        color: #dc2626;
        border-color: #fca5a5;
        }

        .user-dropdown-btn {
        background: none;
        border: none;
        padding: 0;
        cursor: pointer;
        }

        .avatar-circle {
        width: 40px;
        height: 40px;
        background: linear-gradient(135deg, #1e3a8a, #3b82f6);
        color: #ffffff;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        font-weight: 600;
        font-size: 1rem;
        box-shadow: 0 2px 5px rgba(37, 99, 235, 0.3);
        border: 2px solid #ffffff;
        transition: all 0.2s ease;
        }

        .user-dropdown-btn:hover .avatar-circle {
        transform: scale(1.05);
        box-shadow: 0 4px 8px rgba(37, 99, 235, 0.4);
        }

        /* --- Mobile Responsive --- */
        .sidebar-overlay {
        display: none;
        position: fixed;
        top: 0;
        left: 0;
        width: 100vw;
        height: 100vh;
        background-color: rgba(15, 23, 42, 0.5);
        z-index: 1030;
        opacity: 0;
        transition: opacity 0.3s ease;
        }

        @@media (max-width: 768px) {
        .sidebar {
        position: fixed;
        top: 0;
        left: 0;
        height: 100%;
        transform: translateX(-100%);
        }

        .sidebar.show-sidebar {
        transform: translateX(0);
        }

        .sidebar-overlay.show-overlay {
        display: block;
        opacity: 1;
        }

        .top-navbar {
        padding: 0 1rem;
        gap: 10px;
        }

        .search-wrapper {
        width: 100%;
        }

        .search-results-dropdown {
        width: calc(100vw - 2rem);
        position: fixed;
        top: 70px;
        left: 1rem;
        }
        }
    </style>
</head>
<body>

    <div class="app-container">

        <!-- Mobile Overlay -->
        <div class="sidebar-overlay" id="sidebarOverlay"></div>

        <!-- SIDEBAR -->
        <nav class="sidebar" id="sidebar">
            <div class="sidebar-brand">
                <div class="brand-logo-container">
                    <img src="https://cdn.shopify.com/s/files/1/0578/8198/5133/files/Radaropusd_funal.jpg?v=1788239642"
                         alt="Radar One Logo"
                         style="width: 32px; height: 32px; border-radius: 8px; object-fit: cover;">
                    <h5>Radar One</h5>
                </div>
            </div>

            <div class="sidebar-nav">
                <div class="nav-section-title">Work</div>
                <a href="#" class="nav-item-link active">
                    <div class="nav-item-link-content"><i class="bi bi-house"></i> Home</div>
                </a>
                <a href="#" class="nav-item-link">
                    <div class="nav-item-link-content"><i class="bi bi-file-earmark-bar-graph"></i> Reports</div>
                </a>
                <a href="#" class="nav-item-link">
                    <div class="nav-item-link-content"><i class="bi bi-graph-up"></i> Analytics</div>
                </a>

                <div class="nav-section-title">Sales</div>
                <a asp-controller="Accounts" asp-action="Index" class="nav-item-link">
                    <div class="nav-item-link-content"><i class="bi bi-buildings"></i> Accounts</div>
                </a>
                <a asp-controller="Leads" asp-action="Index" class="nav-item-link">
                    <div class="nav-item-link-content"><i class="bi bi-person-lines-fill"></i> Leads</div>
                </a>
                <a asp-controller="Deals" asp-action="Index" class="nav-item-link">
                    <div class="nav-item-link-content"><i class="bi bi-briefcase"></i> Deals</div>
                </a>
                <a asp-controller="Tasks" asp-action="Index" class="nav-item-link">
                    <div class="nav-item-link-content"><i class="bi bi-check2-square"></i> Tasks</div>
                </a>

                <!-- Dropdown: Other Modules -->
                <a href="#otherModulesMenu" data-bs-toggle="collapse" class="nav-item-link" aria-expanded="false">
                    <div class="nav-item-link-content"><i class="bi bi-grid"></i> Other Modules</div>
                    <i class="bi bi-chevron-down toggle-arrow"></i>
                </a>
                <div class="collapse sub-menu" id="otherModulesMenu">
                    <a href="#" class="nav-item-link">Meetings</a>
                    <a asp-controller="Vendors" asp-action="Index" class="nav-item-link">Vendors</a>
                    <a href="#" class="nav-item-link">Outreach Source</a>
                    <a asp-controller="Products" asp-action="Index" class="nav-item-link">Products</a>
                </div>

                <div class="nav-section-title">Administration</div>
                <a asp-controller="Users" asp-action="Index" class="nav-item-link">
                    <div class="nav-item-link-content"><i class="bi bi-people"></i> Users</div>
                </a>
                <a href="#" class="nav-item-link">
                    <div class="nav-item-link-content"><i class="bi bi-shield-lock"></i> Roles</div>
                </a>
                <a href="#" class="nav-item-link">
                    <div class="nav-item-link-content"><i class="bi bi-person-badge"></i> Profiles</div>
                </a>
            </div>
        </nav>

        <!-- MAIN WRAPPER -->
        <div class="main-wrapper">

            <!-- TOP NAVBAR -->
            <header class="top-navbar">
                <!-- Mobile Menu Toggle (Visible only on small screens) -->
                <button class="btn d-md-none" id="mobileMenuBtn">
                    <i class="bi bi-list fs-4"></i>
                </button>

                <!-- Global Search -->
                <div class="search-wrapper">
                    <i class="bi bi-search"></i>
                    <input type="text" id="globalSearchInput" placeholder="Search accounts, leads, deals...">

                    <!-- Search Results Dropdown -->
                    <div class="search-results-dropdown" id="globalSearchResults">
                        <!-- Results dynamically injected here -->
                    </div>
                </div>

                <!-- Right Header Actions -->
                <div class="header-actions d-none d-md-flex">
                    <a href="#" class="btn-signout"><i class="bi bi-box-arrow-right me-1"></i> Sign Out</a>

                    <div class="dropdown">
                        <button class="user-dropdown-btn" type="button" data-bs-toggle="dropdown">
                            <div class="avatar-circle">AU</div>
                        </button>
                        <ul class="dropdown-menu dropdown-menu-end mt-2 shadow-sm border-0">
                            <li class="px-3 py-2 border-bottom mb-1">
                                <strong class="d-block text-dark">Admin User</strong>
                                <small class="text-muted">Administrator</small>
                            </li>
                            <li><a class="dropdown-item py-2" href="#"><i class="bi bi-person me-2 text-muted"></i>My Profile</a></li>
                            <li><a class="dropdown-item py-2" href="#"><i class="bi bi-gear me-2 text-muted"></i>Settings</a></li>
                        </ul>
                    </div>
                </div>
            </header>

            <!-- DYNAMIC PAGE CONTENT -->
            <main class="content-area">
                @RenderBody()
            </main>
        </div>
    </div>

    <!-- GLOBAL SEARCH QUICK VIEW MODAL -->
    <div class="modal fade" id="quickViewModal" tabindex="-1" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content border-0 shadow-lg" style="border-radius: 12px;">
                <div class="modal-header bg-light border-bottom-0 rounded-top" style="padding: 20px 24px;">
                    <h5 class="modal-title fw-bold text-dark d-flex align-items-center gap-2" id="quickViewTitle">
                        <i class="bi bi-file-earmark-text text-primary"></i> Record Details
                    </h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body p-4" id="quickViewBody">
                    <!-- Data injected here via JS -->
                </div>
                <div class="modal-footer bg-light border-top-0 rounded-bottom">
                    <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">Close</button>
                    <a href="#" id="quickViewGoToBtn" class="btn btn-primary px-4">Open Full Record</a>
                </div>
            </div>
        </div>
    </div>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>

    <!-- UI & Search Scripts -->
    <script>
        document.addEventListener("DOMContentLoaded", function() {

        // --- Mobile Sidebar Toggle ---
        const mobileBtn = document.getElementById('mobileMenuBtn');
        const sidebar = document.getElementById('sidebar');
        const overlay = document.getElementById('sidebarOverlay');

        if(mobileBtn) {
        mobileBtn.addEventListener('click', () => {
        sidebar.classList.add('show-sidebar');
        overlay.classList.add('show-overlay');
        });
        }

        if(overlay) {
        overlay.addEventListener('click', () => {
        sidebar.classList.remove('show-sidebar');
        overlay.classList.remove('show-overlay');
        });
        }

        // --- Global Search Logic ---
        const searchInput = document.getElementById('globalSearchInput');
        const resultsDropdown = document.getElementById('globalSearchResults');
        const quickViewModal = new bootstrap.Modal(document.getElementById('quickViewModal'));

        // MOCK DATABASE FOR TESTING VISUALS (Replace this with an actual AJAX fetch call to your backend later)
        const mockDatabase = [
        { id: 1, type: 'Account', name: 'Alpha Tech Solutions', owner: 'Admin User', status: 'Active', icon: 'bi-buildings', badge: 'badge-account' },
        { id: 2, type: 'Account', name: 'Apex Corp', owner: 'Sarah Jones', status: 'Inactive', icon: 'bi-buildings', badge: 'badge-account' },
        { id: 3, type: 'Lead', name: 'Aaron Smith', phone: '+1 555-0192', source: 'Website', icon: 'bi-person-lines-fill', badge: 'badge-lead' },
        { id: 4, type: 'Lead', name: 'Alicia Keys', phone: '+1 555-8832', source: 'Referral', icon: 'bi-person-lines-fill', badge: 'badge-lead' },
        { id: 5, type: 'Deal', name: 'Alpha SaaS Expansion', amount: '₹50,000', stage: 'Negotiation', icon: 'bi-briefcase', badge: 'badge-deal' }
        ];

        // Close dropdown when clicking outside
        document.addEventListener('click', function(e) {
        if (!searchInput.contains(e.target) && !resultsDropdown.contains(e.target)) {
        resultsDropdown.style.display = 'none';
        }
        });

        searchInput.addEventListener('input', function() {
        const query = this.value.toLowerCase().trim();

        if (query.length === 0) {
        resultsDropdown.style.display = 'none';
        return;
        }

        // Show Dropdown with Loading state
        resultsDropdown.style.display = 'block';
        resultsDropdown.innerHTML = '<div class="p-3 text-center text-muted"><div class="spinner-border spinner-border-sm me-2"></div>Searching...</div>';

        // --- INTEGRATION POINT: Replace the setTimeout below with an actual fetch request ---
        // Example: fetch(`/Search/Global?q=${query}`).then(res => res.json()).then(data => renderResults(data));

        setTimeout(() => {
        const filtered = mockDatabase.filter(item => item.name.toLowerCase().includes(query));
        renderResults(filtered);
        }, 300); // Simulate network delay
        });

        function renderResults(data) {
        if (data.length === 0) {
        resultsDropdown.innerHTML = '<div class="p-3 text-center text-muted">No records found.</div>';
        return;
        }

        let html = '';
        data.forEach(item => {
        // Create subtitle based on type
        let subText = '';
        if(item.type === 'Account') subText = `Owner: ${item.owner} • Status: ${item.status}`;
        if(item.type === 'Lead') subText = `Phone: ${item.phone} • Source: ${item.source}`;
        if(item.type === 'Deal') subText = `Value: ${item.amount} • Stage: ${item.stage}`;

        // Build stringified object to pass to onclick
        const itemJson = encodeURIComponent(JSON.stringify(item));

        html += `
        <div class="search-result-item" onclick="openQuickView('${itemJson}')">
        <div class="search-result-icon"><i class="bi ${item.icon}"></i></div>
        <div class="search-result-details">
        <h6>${item.name} <span class="search-badge ${item.badge} ms-2">${item.type}</span></h6>
        <p>${subText}</p>
        </div>
        </div>
        `;
        });

        resultsDropdown.innerHTML = html;
        }

        // Expose function globally so inline onclick can reach it
        window.openQuickView = function(encodedItem) {
        const item = JSON.parse(decodeURIComponent(encodedItem));

        // Hide dropdown
        resultsDropdown.style.display = 'none';
        searchInput.value = '';

        // Set Modal Content
        document.getElementById('quickViewTitle').innerHTML = `<i class="bi ${item.icon} text-primary me-2"></i> ${item.type} Quick View`;

        let bodyHtml = `<h4 class="fw-bold mb-4 text-dark">${item.name}</h4>`;

        if(item.type === 'Account') {
        bodyHtml += `<p><strong>Owner:</strong> ${item.owner}</p><p><strong>Status:</strong> <span class="badge bg-success">${item.status}</span></p>`;
        document.getElementById('quickViewGoToBtn').href = `/Accounts/Edit/${item.id}`; // Assuming ID routing
        } else if(item.type === 'Lead') {
        bodyHtml += `<p><strong>Phone:</strong> ${item.phone}</p><p><strong>Source:</strong> ${item.source}</p>`;
        document.getElementById('quickViewGoToBtn').href = `/Leads/Edit/${item.id}`;
        } else if(item.type === 'Deal') {
        bodyHtml += `<p><strong>Amount:</strong> ${item.amount}</p><p><strong>Stage:</strong> ${item.stage}</p>`;
        document.getElementById('quickViewGoToBtn').href = `/Deals/Edit/${item.id}`;
        }

        document.getElementById('quickViewBody').innerHTML = bodyHtml;
        quickViewModal.show();
        };
        });
    </script>

    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>